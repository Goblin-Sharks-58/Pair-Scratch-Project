import React from 'react';
import {
    createBrowserRouter,
    RouterProvider,
    Route,
    NavLink,
    BrowserRouter as Router, Link, Routes,
} from "react-router-dom";
import './styles.scss';

return (
    <Routes>
        <Route path='' element={ } />
    </Routes>
)
export default App;